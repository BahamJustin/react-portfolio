import React from "react";
import Header from "../components/Header";
import AboutMe from "../components/AboutMe";

const Home = () => {
  return (
    <main>
      <AboutMe />
    </main>
  );
};

export default Home;
